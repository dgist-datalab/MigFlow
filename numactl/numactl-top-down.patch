--- a/numactl.c
+++ b/numactl.c
@@ -40,6 +40,7 @@
 	{"interleave-weight", 1, 0, 'w' },
 	{"preferred", 1, 0, 'p' },
 	{"preferred-many", 1, 0, 'P' },
+	{"top-down", 1, 0, 'K'},
 	{"cpubind", 1, 0, 'c' },
 	{"cpunodebind", 1, 0, 'N' },
 	{"physcpubind", 1, 0, 'C' },
@@ -70,6 +71,7 @@
 		"usage: numactl [--all | -a] [--balancing | -b]\n"
 		"               [--interleave= | -i <nodes>] [--interleave-weight | -w <weight>]\n"
 		"               [--preferred= | -p <node>] [--preferred-many= | -P <nodes>]\n"
+		"               [--top-down= | -K <nodes>]\n"
 		"               [--physcpubind= | -C <cpus>] [--cpunodebind= | -N <nodes>]\n"
 		"               [--membind= | -m <nodes>] [--localalloc | -l] command args ...\n"
 		"               [--localalloc | -l] command args ...\n"
@@ -181,6 +183,9 @@
 	case MPOL_PREFERRED_MANY:
 		printf("%ld (preferred-many)\n",cur);
 		break;
+	case MPOL_TOP_DOWN:
+		printf("%ld (top-down)\n",cur);
+		break;
 	}
 	if (policy == MPOL_INTERLEAVE) {
 		printmask("interleavemask", interleave);
@@ -627,6 +632,25 @@
 			}
 			checkerror("setting preferred node");
 			break;
+		case 'K':
+			checknuma();
+			if (parse_all)
+				mask = numactl_parse_nodestring(optarg, ALL);
+			else
+				mask = numactl_parse_nodestring(optarg, CPUSET);
+			if (!mask) {
+				printf ("<%s> is invalid\n", optarg);
+				usage();
+			}
+			errno = 0;
+			did_node_cpu_parse = 1;
+			numa_set_bind_policy(0);
+			if (shmfd >= 0)
+				complain("--top-down is not supported for shared memory");
+			setpolicy(MPOL_TOP_DOWN);
+			numa_set_top_down(mask);
+			checkerror("setting top-down policy");
+			break;
 		case 'l': /* --local */
 			checknuma();
 			setpolicy(MPOL_LOCAL);
--- a/libnuma.c
+++ b/libnuma.c
@@ -1906,6 +1906,11 @@
 	setpol(MPOL_PREFERRED_MANY, bitmask);
 }
 
+void numa_set_top_down(struct bitmask *bitmask)
+{
+	setpol(MPOL_TOP_DOWN, bitmask);
+}
+
 struct bitmask *numa_preferred_many()
 {
 	return __numa_preferred();
--- a/numa.h
+++ b/numa.h
@@ -201,6 +201,9 @@
 /* Set of nodes to preferably allocate memory from for task. */
 void numa_set_preferred_many(struct bitmask *bitmask);
 
+/* Allocate from the given nodes in order, fastest tier first (MPOL_TOP_DOWN). */
+void numa_set_top_down(struct bitmask *bitmask);
+
 /* Return preferred nodes */
 struct bitmask *numa_preferred_many(void);
 
--- a/numaif.h
+++ b/numaif.h
@@ -36,7 +36,8 @@
 #define MPOL_LOCAL       4
 #define MPOL_PREFERRED_MANY   5
 #define MPOL_INTERLEAVE_WEIGHT 6
-#define MPOL_MAX         7
+#define MPOL_TOP_DOWN 7
+#define MPOL_MAX         8
 
 /* Flags for set_mempolicy, specified in mode */
 #define MPOL_F_NUMA_BALANCING	(1 << 13) /* Optimize with NUMA balancing if possible */
--- a/util.c
+++ b/util.c
@@ -95,7 +95,7 @@
 	{ NULL },
 };
 
-static char *policy_names[] = { "default", "preferred", "bind", "interleave", "local", "preferred-many" };
+static char *policy_names[] = { "default", "preferred", "bind", "interleave", "local", "preferred-many", "interleave-weight", "top-down" };
 
 char *policy_name(int policy)
 {
--- a/versions.ldscript
+++ b/versions.ldscript
@@ -160,6 +160,7 @@
   global:
     numa_has_preferred_many;
     numa_set_preferred_many;
+    numa_set_top_down;
     numa_preferred_many;
     set_mempolicy_node_weight;
     mrange_node_weight;
